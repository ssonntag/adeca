adeca.engine ? adeca.engine = adeca.engine : adeca.engine = {};
adeca.engine.intro = (function() {
    var tick = function(frame) {
    };
    var init = function() {
    };
    return {
        tick: tick,
        init: init
    }
})();
